<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.13">
  <compounddef id="md_docs_getting-started_macos" kind="page">
    <compoundname>md_docs_getting-started_macos</compoundname>
    <title>Installing on macOS</title>
    <detaileddescription>
<para>There are currently two ways to install PROS 3 on macOS. The recommended method uses <ulink url="https://brew.sh/">Homebrew</ulink>, and the other method involves installing components manually.</para><para>The recommended method of installing PROS 3 for macOS involves using <ulink url="https://brew.sh/">Homebrew</ulink>.</para><para><orderedlist>
<listitem><para>If you do not already have Homebrew installed, install it by following the instructions on <ulink url="https://brew.sh">their site</ulink>. This will take a while, and may prompt you to follow some additional instructions.</para></listitem><listitem><para>Once you have Homebrew installed, run brew tap osx-cross/arm &amp;&amp; brew install arm-gcc-bin to register a repository with Homebrew that contains the toolchain used to build PROS projects, and then install the toolchain.</para></listitem><listitem><para>Run brew tap purduesigbots/pros to register the PROS Homebrew repository with Homebrew.</para></listitem><listitem><para>Run brew cask install pros-editor to install the PROS Editor (the CLI will also be installed). This may also take a while.</para></listitem><listitem><para>That&apos;s it! You can now start using PROS 3.</para></listitem></orderedlist>
</para><para><blockquote><para><bold>note</bold></para><para>If you do not want to use the PROS Editor, and instead intend to use only the PROS CLI, substitute the command in step 3 with the following: brew install pros-cli. </para></blockquote></para><para>If you don&apos;t want to use Homebrew to install PROS 3, you can install all the components manually.</para><para><orderedlist>
<listitem><para>Download the latest version of the GNU Arm Embedded Toolchain for macOS from <ulink url="https://developer.arm.com/open-source/gnu-toolchain/gnu-rm/downloads">their site</ulink>.</para></listitem><listitem><para>Once you have downloaded the toolchain, double click the file to extract its contents.</para></listitem><listitem><para>Copy the contents of the gcc-arm-none-eabi-X-20XX-qX-update folder (where the Xs are numbers specific to the version you downloaded) to another folder, for example /usr/local/lib/pros-toolchain.</para></listitem><listitem><para>Now you will need to link the toolchain binaries to somewhere that the system will be able to find them. There are two ways to do this: i) (recommended, easy to update) run <computeroutput>mkdir -p /usr/local/bin/pros-toolchain &amp;&amp; ln -s /usr/local/lib/pros-toolchain/bin/* /usr/local/bin/pros-toolchain</computeroutput> (replacing <computeroutput>/usr/local/lib/pros-toolchain</computeroutput> with the path to the folder you made in step 4 above). Finally, add <computeroutput>/usr/local/bin/pros-toolchain</computeroutput> to the end of your <computeroutput>/etc/paths</computeroutput> file. ii) (easier, less easy to update) simply run <computeroutput>ln -s /usr/local/lib/pros-toolchain/bin/* /usr/local/bin</computeroutput>.</para></listitem></orderedlist>
</para><para><orderedlist>
<listitem><para>Install Python 3.6 or higher from <ulink url="http://python.org">the Python website</ulink>.</para></listitem><listitem><para>Install the CLI by downloading the latest version of the Python Wheel file (.whl) from <ulink url="https://github.com/purduesigbots/pros-cli3/releases/latest">here</ulink>. Once downloaded, run python3 -m pip install/Downloads/pros-cli-v5_3.X.X-py3-none-any.whl (replacing that path with the path to which you downloaded the file).</para></listitem></orderedlist>
</para><para><blockquote><para><bold>note</bold></para><para>this section is optional if you intend to use an editor other than the PROS Editor </para></blockquote></para><para><orderedlist>
<listitem><para>Build and install cquery by following the instructions on [their wiki page](<ulink url="https://github.com/cquery-project/cquery/wiki/Building-cquery">https://github.com/cquery-project/cquery/wiki/Building-cquery</ulink>).</para></listitem><listitem><para>Download the pros-editor-mac.zip file from <ulink url="https://github.com/purduesigbots/atom/releases/latest">our releases page</ulink>. Once downloaded, double click to extract the application, then drag the PROS Editor.app file to your /Applications folder.</para></listitem></orderedlist>
</para><para>Minimum macOS version: 10.8 Minimum Python version: 3.6</para><para>RuntimeError: Click will abort further execution because Python 3 was configured to use ASCII as encoding for the environment.</para><para>If you are using the PROS Editor, open up your init script (File &gt; Init Script) and add the following two lines:</para><para><programlisting><codeline><highlight class="normal">process.env.LANG<sp/>=<sp/>&apos;en_US.utf-8&apos;</highlight></codeline>
<codeline><highlight class="normal">process.env.LC_ALL<sp/>=<sp/>&apos;en_US.utf-8&apos;</highlight></codeline>
</programlisting></para><para>If you are just using the CLI at the Terminal:</para><para><orderedlist>
<listitem><para>Open up your Terminal.</para></listitem><listitem><para>Run cd to make sure you&apos;re in your home directory.</para></listitem><listitem><para>Run touch .bash_profile to make sure you have a shell login configuration file.</para></listitem><listitem><para>Edit the/.bash_profile file in your preferred editor (you can also run open -e .bash_profile to edit it in TextEdit), adding the following two lines at the end:</para></listitem></orderedlist>
</para><para><programlisting><codeline><highlight class="normal">export<sp/>LANG=&quot;en_US.utf-8&quot;</highlight></codeline>
<codeline><highlight class="normal">export<sp/>LC_ALL=&quot;en_US.utf-8&quot;</highlight></codeline>
</programlisting></para><para><orderedlist>
<listitem><para>Run . .bash_profile to reload the file for the current session.</para></listitem></orderedlist>
</para><para>/bin/sh: intercept-c++: command not found</para><para><blockquote><para><bold>note</bold></para><para>This issue should be fixed for PROS CLI versions &gt; 3.1.2 </para></blockquote></para><para><orderedlist>
<listitem><para>Check your PROS CLI version by running prosv5 <ndash/>version. If your version is &lt;= 3.1.2, try updating first to check if that solves your problem. If not, continue with step 2.</para></listitem><listitem><para>Follow steps 1-4 listed above for those using the CLI only. In step 4, however, add the following line instead (replacing the Xs with the numbers found in step 1):</para></listitem></orderedlist>
</para><para><programlisting><codeline><highlight class="normal">export<sp/>PATH=&quot;/usr/local/Cellar/pros-cli/3.X.X/libexec/bin:$PATH&quot;</highlight></codeline>
</programlisting> </para>    </detaileddescription>
  </compounddef>
</doxygen>
