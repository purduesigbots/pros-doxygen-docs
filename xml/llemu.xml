<?xml version='1.0' encoding='UTF-8' standalone='no'?>
<doxygen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:noNamespaceSchemaLocation="compound.xsd" version="1.8.17">
  <compounddef id="llemu" kind="page">
    <compoundname>llemu</compoundname>
    <title>LLEMU (Legacy LCD Emulator)</title>
    <briefdescription>
    </briefdescription>
    <detaileddescription>
<para><simplesect kind="note"><para>For a full list of functions for interacting with the LLEMU, see its <ref refid="group__c-llemu" kindref="compound">C API</ref> and <ref refid="group__cpp-llemu" kindref="compound">C++ API</ref>.</para>
</simplesect>
</para>
<sect2 id="llemu_1autotoc_md52">
<title>Initialization</title>
<para>Initialization of the LLEMU is very simple, it&apos;s just a call to its initialization function at whatever point in the program you would like the LLEMU to start displaying (this will most likely be in <computeroutput><ref refid="main_8h_1a9efe22aaead3a5e936b5df459de02eba" kindref="member">initialize()</ref></computeroutput>).</para>
<para>Initialization is done as such:</para>
</sect2>
<sect2 id="llemu_1autotoc_md53">
<title>Writing to the LLEMU</title>
<para>Writing to the LLEMU is nearly identical to writing to the LCD with <ulink url="../../cortex/tutorials/lcd.html">PROS 2</ulink>. Most writing should be done with the print function, which is analogous to <ulink url="http://www.cplusplus.com/reference/cstdio/printf/">printf</ulink>.</para>
</sect2>
<sect2 id="llemu_1autotoc_md54">
<title>Using the Buttons</title>
<para>Using the buttons can be done in a similar method to <ulink url="../../../cortex/tutorials/lcd.html">PROS 2</ulink> with the <ulink url="../../api/cpp/llemu.html#read-buttons">pros::lcd::read_buttons</ulink> function. See the above example for printing the button readings.</para>
<para>While this is sufficient for most applications, some tasks are easier to perform using the <ulink url="../../api/cpp/llemu.html#register-btn0-cb">pros::lcd::register_btn#_cb</ulink> functions (where # is replaced with 0, 1, or 2 for the left, center, and right buttons respectively). With these function you can assign a function to be called each time that the button is pressed.</para>
<para><simplesect kind="note"><para>Custom LVGL code cannot be displayed at the same time as LLEMU. </para>
</simplesect>
</para>
</sect2>
    </detaileddescription>
  </compounddef>
</doxygen>
